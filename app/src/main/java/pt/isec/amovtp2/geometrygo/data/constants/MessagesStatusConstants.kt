package pt.isec.amovtp2.geometrygo.data.constants

object MessagesStatusConstants {
    // Message to exit a lobby.
    const val EXIT_MESSAGE = "!exit"


    // Start state.
    const val START_STATE = "startState"

    // End state.
    const val END_STATE = "endState"

    // Standby state
    const val STANDBY_STATE = "standbyState"
}